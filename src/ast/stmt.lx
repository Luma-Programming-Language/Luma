@module "ast_stmt"

@use "ast" as AST

#returns_ownership
pub const make_program -> fn (modules: **AstNode, module_count: int, line: int, col: int) *AstNode {
  let n: *ProgramNode = cast<*ProgramNode>(alloc(sizeof<ProgramNode>));

  n.kind = AST::NodeType::PROGRAM;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;

  n.col = col;
  n.modules = modules;
  n.module_count = module_count;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_expr_stmt -> fn (expression: *AstNode, line: int, col: int) *AstNode {
  let n: *ExprStmtNode = cast<*ExprStmtNode>(alloc(sizeof<ExprStmtNode>));

  n.kind = AST::NodeType::STMT_EXPRESSION;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.expression = expression;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_var_decl -> fn (name: *byte, doc_comment: *byte, var_type: *AstNode,
                               initializer: *AstNode, is_mutable: int, is_public: int, line: int, col: int) *AstNode {
  let n: *VarDeclNode = cast<*VarDeclNode>(alloc(sizeof<VarDeclNode>));

  n.kind = AST::NodeType::STMT_VAR_DECL;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.name = name;
  n.doc_comment = doc_comment;
  n.var_type = var_type;
  n.initializer = initializer;
  n.is_mutable = is_mutable;
  n.is_public = is_public;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_const_decl -> fn (name: *byte, doc_comment: *byte, var_type: *AstNode,
                                 initializer: *AstNode, is_public: int, line: int, col: int) *AstNode {
  let n: *VarDeclNode = cast<*VarDeclNode>(alloc(sizeof<VarDeclNode>));

  n.kind = AST::NodeType::STMT_CONST_DECL;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.name = name;
  n.doc_comment = doc_comment;
  n.var_type = var_type;
  n.initializer = initializer;
  n.is_mutable = 0;
  n.is_public = is_public;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_func_decl -> fn (name: *byte, doc_comment: *byte, param_names: **byte, param_types: **AstNode,
                                param_count: int, return_type: *AstNode, is_public: int, body: *AstNode,
                                returns_ownership: int, takes_ownership: int,
                                forward_declared: int, line: int, col: int) *AstNode {
  let n: *FuncDeclNode = cast<*FuncDeclNode>(alloc(sizeof<FuncDeclNode>));

  n.kind = AST::NodeType::STMT_FUNCTION;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.name = name;
  n.doc_comment = doc_comment;
  n.param_names = param_names;
  n.param_types = param_types;
  n.param_count = param_count;
  n.return_type = return_type;
  n.is_public = is_public;
  n.body = body;
  n.returns_ownership = returns_ownership;
  n.takes_ownership = takes_ownership;
  n.forward_declared = forward_declared;
  n.dll_import = 0;
  n.dll_name = cast<*byte>(0);
  n.dll_callconv = cast<*byte>(0);
  n.lib_import = 0;
  n.lib_name = cast<*byte>(0);

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_struct_decl -> fn (name: *byte, doc_comment: *byte, public_members: **AstNode,
                                  public_count: int, private_members: **AstNode, private_count: int,
                                  is_public: int, line: int, col: int) *AstNode {
  let n: *StructDeclNode = cast<*StructDeclNode>(alloc(sizeof<StructDeclNode>));

  n.kind = AST::NodeType::STMT_STRUCT;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.name = name;
  n.doc_comment = doc_comment;
  n.public_members = public_members;
  n.public_count = public_count;
  n.private_members = private_members;
  n.private_count = private_count;
  n.is_public = is_public;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_field_decl -> fn (name: *byte, doc_comment: *byte, type_node: *AstNode,
                                 function: *AstNode, is_public: int, line: int, col: int) *AstNode {
  let n: *FieldDeclNode = cast<*FieldDeclNode>(alloc(sizeof<FieldDeclNode>));

  n.kind = AST::NodeType::STMT_FIELD_DECL;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.name = name;
  n.doc_comment = doc_comment;
  n.type_node = type_node;
  n.function = function;
  n.is_public = is_public;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_enum_decl -> fn (name: *byte, doc_comment: *byte, members: **byte,
                                member_count: int, is_public: int, line: int, col: int) *AstNode {
  let n: *EnumDeclNode = cast<*EnumDeclNode>(alloc(sizeof<EnumDeclNode>));

  n.kind = AST::NodeType::STMT_ENUM;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.name = name;
  n.doc_comment = doc_comment;
  n.members = members;
  n.member_count = member_count;
  n.is_public = is_public;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_if_stmt -> fn (condition: *AstNode, then_stmt: *AstNode, elif_stmts: **AstNode,
                              elif_count: int, else_stmt: *AstNode, line: int, col: int) *AstNode {
  let n: *IfStmtNode = cast<*IfStmtNode>(alloc(sizeof<IfStmtNode>));

  n.kind = AST::NodeType::STMT_IF;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.condition = condition;
  n.then_stmt = then_stmt;
  n.elif_stmts = elif_stmts;
  n.elif_count = elif_count;
  n.else_stmt = else_stmt;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_loop_stmt -> fn (condition: *AstNode, optional: *AstNode, body: *AstNode,
                                initializer: **AstNode, init_count: int, line: int, col: int) *AstNode {
  let n: *LoopStmtNode = cast<*LoopStmtNode>(alloc(sizeof<LoopStmtNode>));

  n.kind = AST::NodeType::STMT_LOOP;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.condition = condition;
  n.optional = optional;
  n.body = body;
  n.initializer = initializer;
  n.init_count = init_count;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_return_stmt -> fn (value: *AstNode, line: int, col: int) *AstNode {
  let n: *ReturnStmtNode = cast<*ReturnStmtNode>(alloc(sizeof<ReturnStmtNode>));

  n.kind = AST::NodeType::STMT_RETURN;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.value = value;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_block -> fn (statements: **AstNode, stmt_count: int, line: int, col: int) *AstNode {
  let n: *BlockNode = cast<*BlockNode>(alloc(sizeof<BlockNode>));

  n.kind = AST::NodeType::STMT_BLOCK;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.statements = statements;
  n.stmt_count = stmt_count;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_print_stmt -> fn (expressions: **AstNode, expr_count: int, ln: int, line: int, col: int) *AstNode {
  let n: *PrintStmtNode = cast<*PrintStmtNode>(alloc(sizeof<PrintStmtNode>));

  n.kind = AST::NodeType::STMT_PRINT;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.expressions = expressions;
  n.expr_count = expr_count;
  n.ln = ln;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_break_continue -> fn (is_continue: int, line: int, col: int) *AstNode {
  let n: *BreakContinueNode = cast<*BreakContinueNode>(alloc(sizeof<BreakContinueNode>));

  n.kind = AST::NodeType::STMT_BREAK_CONTINUE;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.is_continue = is_continue;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_defer_stmt -> fn (statement: *AstNode, line: int, col: int) *AstNode {
  let n: *DeferNode = cast<*DeferNode>(alloc(sizeof<DeferNode>));

  n.kind = AST::NodeType::STMT_DEFER;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.statement = statement;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_switch_stmt -> fn (condition: *AstNode, cases: **AstNode, case_count: int,
                                  default_case: *AstNode, line: int, col: int) *AstNode {
  let n: *SwitchNode = cast<*SwitchNode>(alloc(sizeof<SwitchNode>));

  n.kind = AST::NodeType::STMT_SWITCH;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.condition = condition;
  n.cases = cases;
  n.case_count = case_count;
  n.default_case = default_case;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_impl_stmt -> fn (function_name_list: **byte, function_type_list: **AstNode, struct_name_list: **byte,
                                function_name_count: int, struct_name_count: int, body: *AstNode,
                                line: int, col: int) *AstNode {
  let n: *ImplNode = cast<*ImplNode>(alloc(sizeof<ImplNode>));

  n.kind = AST::NodeType::STMT_IMPL;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.function_name_list = function_name_list;
  n.function_type_list = function_type_list;
  n.struct_name_list = struct_name_list;
  n.function_name_count = function_name_count;
  n.struct_name_count = struct_name_count;
  n.body = body;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_case_stmt -> fn (values: **AstNode, value_count: int, body: *AstNode, line: int, col: int) *AstNode {
  let n: *CaseNode = cast<*CaseNode>(alloc(sizeof<CaseNode>));

  n.kind = AST::NodeType::STMT_CASE;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.values = values;
  n.value_count = value_count;
  n.body = body;

  return cast<*AstNode>(n);
}

#returns_ownership
pub const make_default_stmt -> fn (body: *AstNode, line: int, col: int) *AstNode {
  let n: *DefaultNode = cast<*DefaultNode>(alloc(sizeof<DefaultNode>));

  n.kind = AST::NodeType::STMT_DEFAULT;
  n.category = AST::NodeCategory::Node_Category_STMT;
  n.line = line;
  n.col = col;

  n.body = body;
  
  return cast<*AstNode>(n);
}

// error-type: Type Error
@module "main"

const do_nothing -> fn () void {
    return;
}

pub const main -> fn (argc: int, argv: **byte) int {
    let x: int = cast<int>(do_nothing());
    return 0;
}

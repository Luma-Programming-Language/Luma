@module "main"

// pub const foo -> fn () [int] {
//  return [];
// }

pub const main -> fn () int {
  let x: int = 20;

  if (x > 10) output(x, "\n");
  else output("Hello, World!\n");
  
  return 0;
}

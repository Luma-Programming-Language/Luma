@module "main"

// example of using a function type
const add -> fn (x: fn (int) int, y: int) int {
    return x(y) + y;
}

const x -> fn (int) int {
    return 2;
}

pub const main -> fn () int {
    output(add(x, 3)); // should output 8
    return 0;
}
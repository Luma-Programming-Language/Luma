@module "main"

@use "std_math" as math

// example of using a function type
const add -> fn (x: fn (int, int) int, y: int) int {
    return x(y, y) + 3;
}

pub const main -> fn () int {
    let x: fn (int, int) int = math::mod;
    output(add(x, 3));
    return 0;
}
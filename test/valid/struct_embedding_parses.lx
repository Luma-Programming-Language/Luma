@module "main"

const Base -> struct {
pub:
  id: i64,
};

const Derived -> struct {
pub:
  ...Base,
  name: *byte,
};

pub const main -> fn (argc: i64, argv: **byte) i64 {
    let d: Derived = Derived { id: 1, name: "hi" };
    output(d.name, "\n");
    return 0;
}

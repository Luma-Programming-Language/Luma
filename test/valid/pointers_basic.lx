@module "main"

pub const main -> fn (argc: i64, argv: **byte) i64 {
    let x: i64 = 10;
    let px: *i64 = &x;
    let y: i64 = *px;

    let arr: *i64 = cast<*i64>(alloc(sizeof<i64> * 4));
    let p2: *i64 = arr + 1;
    free(arr);

    output(y, "\n");
    return 0;
}

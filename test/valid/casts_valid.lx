@module "main"

pub const main -> fn (argc: i64, argv: **byte) i64 {
    let i: i64 = 42;
    let f: f32 = cast<f32>(i);
    let back: i64 = cast<i64>(f);

    let p: *i64 = cast<*i64>(alloc(sizeof<i64>));
    let addr: i64 = cast<i64>(p);
    output(addr, "\n");
    free(p);

    return 0;
}

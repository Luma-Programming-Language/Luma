@module "main"

const Color -> enum { Red, Green, Blue };

const describe -> fn (c: Color) i64 {
    let n: i64 = c;
    return n;
}

pub const main -> fn (argc: i64, argv: **byte) i64 {
    let c: Color = Color::Green;
    let n: i64 = describe(c);
    output(n, "\n");
    return 0;
}

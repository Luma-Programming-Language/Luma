@module "main"

pub const main -> fn (argc: i64, argv: **byte) i64 {
    let p: *byte = cast<*byte>(alloc(8));
    if (argc > 1) {
        free(p);
        return 0;
    }
    free(p);
    return 0;
}

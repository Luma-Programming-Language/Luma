@module "main"

#returns_ownership
const make_buf -> fn () *byte {
    let p: *byte = cast<*byte>(alloc(8));
    return p;
}

pub const main -> fn (argc: i64, argv: **byte) i64 {
    let p: *byte = make_buf();
    free(p);
    return 0;
}

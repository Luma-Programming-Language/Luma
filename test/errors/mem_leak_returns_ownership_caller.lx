// error-type: Memory Leak
@module "main"

#returns_ownership
const make_buf -> fn () *byte {
    return cast<*byte>(alloc(8));
}

pub const main -> fn (argc: i64, argv: **byte) i64 {
    let p: *byte = make_buf();
    return 0;
}

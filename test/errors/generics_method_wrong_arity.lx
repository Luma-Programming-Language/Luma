// error-type: Generics Error

@module "main"

const Thing -> struct {
  x: i64,

  static make -> fn<T> (kind: i64, line: i64) T {
    return cast<T>(0);
  },
};

pub const main -> fn (argc: i64, argv: **byte) i64 {
  let a: *Thing = Thing::make<*Thing, i64>(1, 2);
  return 0;
}